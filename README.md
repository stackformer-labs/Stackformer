we only get emb_dim, n_head, max_token_id and dropout as input to our model

1) first complete work on attention mechanism:
    - self attention
    - multi head attention
    - KV catch
    - flash attention

2) Normalization and skip connection:
- layer normalization
- RMS normalization

3) Feed forward layer:
    - standard feed forward
    - GLU feed forward

4) tokenization and embedding

5) position embedding

6) arch like gpt2model, llama model arch

7) improve trainers add things like trainer in trl